using System;
using HarmonyLib;
using ColossalFramework.Math;

namespace NaturalDisastersRenewal.Patches
{
    //[HarmonyPatch(typeof(DisasterHelpers))]
    //[HarmonyPatch("DestroyBuildings")]
    //[HarmonyPatch(new Type[] { typeof(InstanceManager.Group), typeof(Segment3), typeof(float), typeof(uint), typeof(uint) })]
    //class DestroyBuildings1Patch
    //{
    //    static bool Prefix()
    //    {
    //        return false;
    //    }
    //}
}
